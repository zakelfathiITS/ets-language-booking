# Application and system caches use the filesystem (var/cache). The rate
# limiter behind login throttling stores its counters in the app cache.
framework:
    cache:
        app: cache.adapter.filesystem
        system: cache.adapter.system
