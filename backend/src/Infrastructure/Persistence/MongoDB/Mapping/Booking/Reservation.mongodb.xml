<?xml version="1.0" encoding="UTF-8"?>
<!--
    Collection "reservations": (id, session_id, user_id, reservation date).
    session_id and user_id are stored as ObjectIds, like the _id they point to.
-->
<doctrine-mongo-mapping xmlns="http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping"
                        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                        xsi:schemaLocation="http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping
                                            https://www.doctrine-project.org/schemas/odm/doctrine-mongo-mapping.xsd">

    <document name="App\Domain\Booking\Reservation" collection="reservations">
        <id field-name="id" />

        <field field-name="sessionId" name="session_id" type="object_id" />
        <field field-name="userId" name="user_id" type="object_id" />
        <field field-name="reservedAt" name="reserved_at" type="date_immutable" />

        <indexes>
            <!-- A user books a given session at most once, even under concurrent requests. -->
            <index name="uniq_reservations_session_user" unique="true">
                <key name="sessionId" order="asc" />
                <key name="userId" order="asc" />
            </index>
            <!-- "My reservations", most recent first. -->
            <index name="idx_reservations_user_reserved_at">
                <key name="userId" order="asc" />
                <key name="reservedAt" order="desc" />
            </index>
        </indexes>
    </document>
</doctrine-mongo-mapping>
