<?xml version="1.0" encoding="UTF-8"?>
<!--
    Collection "users": (id, name, email, hashed password).
    Field names are snake_case in MongoDB, camelCase in PHP.
-->
<doctrine-mongo-mapping xmlns="http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping"
                        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                        xsi:schemaLocation="http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping
                                            https://www.doctrine-project.org/schemas/odm/doctrine-mongo-mapping.xsd">

    <document name="App\Domain\Identity\User" collection="users">
        <id field-name="id" />

        <field field-name="name" name="name" type="string" />
        <field field-name="email" name="email" type="string" />
        <field field-name="passwordHash" name="password" type="string" />
        <field field-name="roles" name="roles" type="collection" />
        <field field-name="createdAt" name="created_at" type="date_immutable" />
        <field field-name="updatedAt" name="updated_at" type="date_immutable" />

        <indexes>
            <!-- Emails are stored lower-cased, so this index makes them unique case-insensitively. -->
            <index name="uniq_users_email" unique="true">
                <key name="email" order="asc" />
            </index>
        </indexes>
    </document>
</doctrine-mongo-mapping>
